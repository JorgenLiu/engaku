# Project Rules

<!-- NOTE: This file is NOT auto-filled by `engaku scan` or any automated tool.
     Rules and constraints must come from you (the developer). Add them manually
     here, or let the dev agent extract them from your conversations. -->

## Code Style
<!-- Add code style constraints here -->
<!-- e.g., "Always use 2-space indentation in TypeScript." -->
<!-- e.g., "Never use inline styles — use Tailwind utility classes." -->

## Project Constraints
<!-- Add hard project constraints here -->
<!-- e.g., "Python >=3.8 only. No 3.9+ syntax." -->
<!-- e.g., "Zero third-party dependencies. stdlib only." -->

## Forbidden
<!-- Things that must never happen -->
<!-- e.g., "Never commit secrets or API keys." -->
<!-- e.g., "Never overwrite user data without an explicit backup step." -->

## Agent Configuration
<!-- Model assignments live in .ai/engaku.json. Run `engaku apply` to push them into agent frontmatter. -->
- MAX_CHARS for module knowledge body: 1200 (frontmatter excluded).
