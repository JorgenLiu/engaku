{
  "name": "gitlab",
  "description": "GitLab repository, issues, and MR context via GitLab remote MCP endpoint",
  "upstream": "https://docs.gitlab.com/user/gitlab_duo/mcp/",
  "last_verified": "2026-05-18",
  "server": {
    "type": "http",
    "url": "https://<gitlab.example.com>/api/v4/mcp"
  },
  "tool_wildcard": "gitlab/*",
  "default_agents": ["coder", "planner", "reviewer"],
  "notes": "Replace https://<gitlab.example.com> with your GitLab instance URL (e.g. https://gitlab.com or your org self-hosted URL). Authenticate via a GitLab personal access token in VS Code. Local stdio fallback: npx -y @zereight/mcp-gitlab@latest"
}
