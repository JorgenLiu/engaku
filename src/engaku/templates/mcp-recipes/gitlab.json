{
  "name": "gitlab",
  "description": "GitLab repository, issues, and MR context via GitLab MCP server",
  "upstream": "https://github.com/zereight/mcp-gitlab",
  "last_verified": "2026-05-18",
  "server": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@zereight/mcp-gitlab@latest"]
  },
  "tool_wildcard": "gitlab/*",
  "default_agents": ["coder", "planner", "reviewer"],
  "notes": "Minimal config — add auth and instance details to .vscode/mcp.json after install. Set env GITLAB_PERSONAL_ACCESS_TOKEN and GITLAB_API_URL (for self-hosted) in the server env block."
}
